<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Posts</title>
</head>

<body>
    <h1>Let's create a New Blog Post!</h1>

    <form action="/new-post" method="POST">
        <label for="title">What do you want to call your blog?</label>
        <input type="text" id="title" name="title" required><br><br>

        <label for="author">Who's writing this?</label>
        <input type="text" id="author" name="author" required><br><br>

        <label for="content">What are we writing about? Tell us!</label><br>
        <textarea id="content" name="content" rows="5" cols="30" required></textarea><br><br>

        <button type="submit">Post!</button>
    </form>

    <h2>All Posts</h2>
    <ul>
        <% posts.forEach(function(post) { %>
            <li>
                <h2><%= post.title %></h2>
                <p>By <%= post.creator_name %> on <%= post.date_created %></p>
                <p><%= post.body %></p>
                <a href="/edit/<%= post.blog_id %>">Edit</a>
                <form action="/delete/<%= post.blog_id %>" method="POST">
                    <button type="submit">Delete</button>
                </form>
            </li>
        <% }); %>
    </ul>    

</body>
</html>